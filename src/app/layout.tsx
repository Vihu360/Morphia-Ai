import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
	title: "Morphia | Ai platform designed to help brands grow",
	description: "Morphia is a cutting-edge platform designed to help brands grow through creative social media posts, competitor analysis, and performance insights. Harness the power of AI-driven tools to transform your brand's online presence, engage audiences, and stay ahead in the digital landscape.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
				{children}
				<Toaster/>
      </body>
    </html>
  );
}
